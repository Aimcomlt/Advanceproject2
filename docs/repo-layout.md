# Repository layout

## Active paths
- `game/` — active game application.
- `docs/` — active architecture and developer docs.
- `AGENTS.md`, `execplan.md` — mission and implementation roadmap.

## Archived paths
- `legacy/` — archived blockchain/literary assets from earlier repository identity.

## Default developer flow
Run all standard commands from repository root; each delegates into `game/`.
